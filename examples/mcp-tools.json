[
  {
    "name": "get_user_profile",
    "description": "Retrieves detailed user profile information from the database including personal details, preferences, and account settings",
    "inputSchema": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique identifier for the user"
        },
        "includePreferences": {
          "type": "boolean",
          "description": "Whether to include user preferences in the response"
        }
      },
      "required": ["userId"]
    }
  },
  {
    "name": "search_products",
    "description": "Searches the product catalog using various filters and returns matching products with their details",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query string"
        },
        "category": {
          "type": "string",
          "description": "Filter by product category",
          "enum": ["electronics", "clothing", "books", "home"]
        },
        "maxPrice": {
          "type": "number",
          "description": "Maximum price filter in USD"
        },
        "inStock": {
          "type": "boolean",
          "description": "Only show products that are in stock"
        }
      },
      "required": ["query"]
    }
  }
]
